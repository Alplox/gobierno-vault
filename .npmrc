# @astrojs/tailwind@6 declara peer astro ^3||^4||^5, pero el proyecto usa Astro 7.
# El build funciona correctamente con esta combinación (el lockfile ya la resuelve);
# legacy-peer-deps evita que `npm clean-install` en CI falle por validación estricta de peers.
legacy-peer-deps=true
