---
const { id = '', index, href = `/sources/${id}`, title = `Referencia ${index}` } = Astro.props;
---

<sup class="text-blue-600 font-mono text-xs">
  <a href={href} title={title}>[{index}]</a>
</sup>
